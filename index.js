const FfmpegCommand = require("fluent-ffmpeg");
const command = new FfmpegCommand();

console.log(
  "Hi, I’m TransWarp, a place to share learnings about FFMpeg and its various NPM wrappers and libraries. The idea being that demuxing, concatenating, compositing (yes John, I mean composing in this case, because of OST ;) , transcoding, and rendering..."
);
